package com.example.llamaapp.ui.theme

import androidx.compose.ui.graphics.Color

// Primary palette — dark, focused LLM chat app
val Background = Color(0xFF0F0F1A)
val Surface = Color(0xFF1A1A2E)
val SurfaceVariant = Color(0xFF252540)
val Primary = Color(0xFF00BFA5)        // Teal accent
val PrimaryVariant = Color(0xFF00897B)
val Secondary = Color(0xFF7B61FF)       // Purple for secondary actions
val OnBackground = Color(0xFFE8E8F0)
val OnSurface = Color(0xFFD0D0E8)
val OnPrimary = Color(0xFF000000)

// Chat bubble colors
val UserBubbleColor = Color(0xFF1E3A5F)      // Dark blue — user messages
val AiBubbleColor = Color(0xFF1A2A1A)        // Dark green tint — AI messages

// Thinking block
val ThinkingBlockBackground = Color(0xFF1A1530)  // Slightly purple-tinted dark
val ThinkingBlockBorder = Color(0xFF4A3F7A)
val ThinkingBlockText = Color(0xFFB0A8D8)

// Status colors
val ErrorColor = Color(0xFFCF6679)
val SuccessColor = Color(0xFF4CAF50)
val WarningColor = Color(0xFFFF9800)

// HUD overlay
val HudBackground = Color(0xCC000000)        // Semi-transparent black
val HudText = Color(0xFF00FF88)              // Monospace green for t/s readout
