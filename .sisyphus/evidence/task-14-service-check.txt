=== QA Check 1: FOREGROUND_SERVICE_TYPE_DATA_SYNC ===
            startForeground(NOTIF_ID, notification, ServiceInfo.FOREGROUND_SERVICE_TYPE_DATA_SYNC)

=== QA Check 2: PARTIAL_WAKE_LOCK ===
        wakeLock = pm.newWakeLock(PowerManager.PARTIAL_WAKE_LOCK, "LlamaApp::InferenceLock")

=== QA Check 3: SDK_INT >= 31 ===
        if (Build.VERSION.SDK_INT >= 31) {
        if (Build.VERSION.SDK_INT >= 31) {

=== QA Check 4: @AndroidEntryPoint ===
@AndroidEntryPoint

=== QA Check 5: createNotificationChannel / NotificationChannel ===
        createNotificationChannel()
    private fun createNotificationChannel() {
            val channel = NotificationChannel(
            nm.createNotificationChannel(channel)
